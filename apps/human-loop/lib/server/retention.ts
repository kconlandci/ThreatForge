/** Matches the privacy notice: data is deleted 24 months after the player last played. */
export const RETENTION_MONTHS = 24;
/** The automatic purge runs at most this often per server instance (the daily cron forces it). */
export const PURGE_EVERY_MS = 24 * 60 * 60 * 1000;
